obs 1
npm init -y INICIAR O PROJETO JAVASCRIPT
npm install express AQUI EU INSTALO O MEU FRAMEWORK EXPRESS
npm install nodemon PARA EXECUTAR O SCRIP DO SERVIDOR
npm install uuid FUNCIONALIDADE PARA GERAR ID

obs 2
passo a passo da github:
1 git init somente para INICIAR
2 git status
3 git add .
4 git commit -m "escrever nome do commit"
5 git branch -M escrever nome da branch
6 git remote add origin <link_para_o_seu_repositorio>
7 para o primeiro push deve indicar oque vai subir git push -u origin main
8 quando ja tiver subido e só dar git push
- depois que fez a primeira vez so precisa usar  os topicos 2,3,4,8.

obs3
metodos http
1 get (listar)
2 post (criar)
3 put (atualizar)
4 delete (excluir)

obs4
para acionar o terminal digitar (npm start)

obs5
para parar o terminal (control c)

obs6
para gerar os ids e identificar os usuarios (npm install uuid)

obs7
v4 e o id que ira ser gerado dentro da biblioteca uuid.

obs8
request: de onde vem as requisições 
    1 request.params: serve para passar parametros de busca, por exemplo (id,cpf,email,etc...)
    2 request.body: aonde fica armazenada as informações que estão vindo do front para o back end.
response: como retorna o evento


obs9
findIndex é uma função usado para procurar uma informação dentro do banco de dados.
